# Review Package Task 3
BASE: d7b8101f5cccc5b42f0aac6b6efe078677d7fe8d
HEAD: 47120b497abcff14529dec36fe3109883d1b93fb

## Commits
47120b4 feat: add Xiaohongshu cookie setting on profile page

## Stat
 .../downloader/data/local/SettingsRepository.kt    |  8 +++  .../douyin/downloader/ui/profile/ProfileScreen.kt  | 61 +++++++++++++++++++++-  .../downloader/ui/profile/ProfileViewModel.kt      |  8 +++  3 files changed, 76 insertions(+), 1 deletion(-)

## Diff
diff --git a/app/src/main/java/com/douyin/downloader/data/local/SettingsRepository.kt b/app/src/main/java/com/douyin/downloader/data/local/SettingsRepository.kt index acda17a..92c1cce 100644 --- a/app/src/main/java/com/douyin/downloader/data/local/SettingsRepository.kt +++ b/app/src/main/java/com/douyin/downloader/data/local/SettingsRepository.kt @@ -11,20 +11,21 @@ import kotlinx.coroutines.flow.Flow  import kotlinx.coroutines.flow.map  import javax.inject.Inject  import javax.inject.Singleton    /**   * 鐢ㄦ埛璁剧疆锛欴ataStore 鎸佷箙鍖栥€?  *   * - [defaultQuality]: 榛樿涓嬭浇娓呮櫚搴︼紙auto / highest / lowest锛?  * - [maxConcurrent]: 鍚屾椂涓嬭浇鏁帮紙2 / 3 / 4锛?  * - [subdirName]: MediaStore.Downloads 涓嬬殑瀛愮洰褰曞悕 + * - [xhsCookie]: 灏忕孩涔?Cookie 鍘熸枃锛堣В鏋愬皬绾功鏃堕渶瑕侊級   */  @Singleton  class SettingsRepository @Inject constructor(      @ApplicationContext private val context: Context,  ) {      private val Context.dataStore by preferencesDataStore(name = "settings")        enum class DefaultQuality(val key: String) {          Auto("auto"),          Highest("highest"), @@ -33,39 +34,46 @@ class SettingsRepository @Inject constructor(          companion object {              fun fromKey(key: String?): DefaultQuality =                  entries.firstOrNull { it.key == key } ?: Auto          }      }        data class Settings(          val defaultQuality: DefaultQuality = DefaultQuality.Auto,          val maxConcurrent: Int = 2,          val subdirName: String = "DYSave", +        val xhsCookie: String = "",      )        private object Keys {          val DEFAULT_QUALITY = stringPreferencesKey("default_quality")          val MAX_CONCURRENT = intPreferencesKey("max_concurrent")          val SUBDIR_NAME = stringPreferencesKey("subdir_name") +        val XHS_COOKIE = stringPreferencesKey("xhs_cookie")      }        val flow: Flow<Settings> = context.dataStore.data.map { prefs ->          Settings(              defaultQuality = DefaultQuality.fromKey(prefs[Keys.DEFAULT_QUALITY]),              maxConcurrent = (prefs[Keys.MAX_CONCURRENT] ?: 2).coerceIn(1, 6),              subdirName = prefs[Keys.SUBDIR_NAME]?.takeIf { it.isNotBlank() } ?: "DYSave", +            xhsCookie = prefs[Keys.XHS_COOKIE] ?: "",          )      }        suspend fun setDefaultQuality(value: DefaultQuality) {          context.dataStore.edit { it[Keys.DEFAULT_QUALITY] = value.key }      }        suspend fun setMaxConcurrent(value: Int) {          context.dataStore.edit { it[Keys.MAX_CONCURRENT] = value.coerceIn(1, 6) }      }        suspend fun setSubdirName(value: String) {          val cleaned = value.trim().ifEmpty { "DYSave" }          context.dataStore.edit { it[Keys.SUBDIR_NAME] = cleaned }      } + +    suspend fun setXhsCookie(value: String) { +        context.dataStore.edit { it[Keys.XHS_COOKIE] = value.trim() } +    }  } diff --git a/app/src/main/java/com/douyin/downloader/ui/profile/ProfileScreen.kt b/app/src/main/java/com/douyin/downloader/ui/profile/ProfileScreen.kt index 24392f8..594110e 100644 --- a/app/src/main/java/com/douyin/downloader/ui/profile/ProfileScreen.kt +++ b/app/src/main/java/com/douyin/downloader/ui/profile/ProfileScreen.kt @@ -98,21 +98,36 @@ fun ProfileScreen(viewModel: ProfileViewModel = hiltViewModel()) {                  subtitle = "鍦ㄧ郴缁熶笅杞界洰褰曚笅鐨勫瓙鏂囦欢澶瑰悕锛涢噸鍚悗鐢熸晥",                  icon = Icons.Filled.Create,              ) {                  SubdirEditor(                      current = settings.subdirName,                      onChange = viewModel::onSubdirNameChanged,                  )              }          }   -        // 4) 鍏充簬 +        // 4) 灏忕孩涔?Cookie +        item { +            SettingCard( +                title = "灏忕孩涔?Cookie", +                subtitle = "瑙ｆ瀽灏忕孩涔︽椂闇€瑕侊紱浠庢祻瑙堝櫒鐧诲綍灏忕孩涔﹀悗澶嶅埗 Cookie锛屽師鏂囩矘璐村嵆鍙?, +                icon = Icons.Filled.Create, +            ) { +                XhsCookieEditor( +                    savedCookie = settings.xhsCookie, +                    onSave = viewModel::onXhsCookieSaved, +                    onClear = viewModel::onXhsCookieCleared, +                ) +            } +        } + +        // 5) 鍏充簬          item {              AboutCard()          }      }  }    // ----------------- 鍖哄潡缁勪欢 -----------------    @Composable  private fun SectionTitle(text: String) { @@ -189,20 +204,64 @@ private fun <T> SegmentedRow(                  selected = value == current,                  onClick = { onChange(value) },                  shape = SegmentedButtonDefaults.itemShape(index, options.size),              ) {                  Text(label, style = MaterialTheme.typography.labelLarge)              }          }      }  }   +@Composable +private fun XhsCookieEditor( +    savedCookie: String, +    onSave: (String) -> Unit, +    onClear: () -> Unit, +) { +    var text by remember { mutableStateOf("") } +    OutlinedTextField( +        value = text, +        onValueChange = { text = it }, +        modifier = Modifier.fillMaxWidth(), +        minLines = 3, +        maxLines = 5, +        shape = RoundedCornerShape(12.dp), +        placeholder = { Text("绮樿创 Cookie 鍘熸枃") }, +    ) +    if (savedCookie.isNotBlank()) { +        Spacer(Modifier.height(8.dp)) +        Text( +            text = "宸查厤缃?, +            style = MaterialTheme.typography.bodySmall, +            color = MaterialTheme.colorScheme.primary, +        ) +    } +    Spacer(Modifier.height(8.dp)) +    Row(horizontalArrangement = Arrangement.spacedBy(8.dp)) { +        TextButton( +            onClick = { onSave(text) }, +            enabled = text.isNotBlank(), +        ) { +            Text("淇濆瓨") +        } +        TextButton( +            onClick = { +                text = "" +                onClear() +            }, +            enabled = savedCookie.isNotBlank() || text.isNotBlank(), +        ) { +            Text("娓呯┖") +        } +    } +} +  @Composable  private fun SubdirEditor(current: String, onChange: (String) -> Unit) {      var text by remember(current) { mutableStateOf(current) }      Row(verticalAlignment = Alignment.CenterVertically) {          OutlinedTextField(              value = text,              onValueChange = { text = it },              modifier = Modifier.weight(1f),              singleLine = true,              shape = RoundedCornerShape(12.dp), diff --git a/app/src/main/java/com/douyin/downloader/ui/profile/ProfileViewModel.kt b/app/src/main/java/com/douyin/downloader/ui/profile/ProfileViewModel.kt index f369a62..4097bff 100644 --- a/app/src/main/java/com/douyin/downloader/ui/profile/ProfileViewModel.kt +++ b/app/src/main/java/com/douyin/downloader/ui/profile/ProfileViewModel.kt @@ -36,11 +36,19 @@ class ProfileViewModel @Inject constructor(      fun onMaxConcurrentChanged(value: Int) {          viewModelScope.launch {              settingsRepository.setMaxConcurrent(value)              downloadManager.setMaxConcurrent(value)          }      }        fun onSubdirNameChanged(value: String) {          viewModelScope.launch { settingsRepository.setSubdirName(value) }      } + +    fun onXhsCookieSaved(value: String) { +        viewModelScope.launch { settingsRepository.setXhsCookie(value) } +    } + +    fun onXhsCookieCleared() { +        viewModelScope.launch { settingsRepository.setXhsCookie("") } +    }  }
